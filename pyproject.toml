[project]
name = "hands"
version = "0.1.0"
description = "Robotic finger mechanism — parametric 3D skeleton, kinematic solver, and interactive visualization"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "fastapi>=0.136.3",
    "uvicorn>=0.49.0",
]
